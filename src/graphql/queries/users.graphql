#import "../fragments/posts.graphql"
#import "../fragments/users.graphql"

query User($username: String!) {
  User(username: $username) {
    ...UserProfile
    posts(orderBy: createdAt_DESC) {
      id
      createdAt
      body
      ...UserPostInfo
      ...PostComments
    }
  }
}